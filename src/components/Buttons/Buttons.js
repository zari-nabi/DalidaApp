import React from "react";
import { TouchableOpacity,Text } from "react-native";

const Buttons=()=>(
    <TouchableOpacity>
        <Text>Dalida</Text>
    </TouchableOpacity>
);

export default Buttons;